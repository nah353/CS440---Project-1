{
  "name": "nau-recipe-app-root",
  "private": true,
  "scripts": {
    "setup": "node setup-env.js && npm install && npm run install-client && npm run install-server-ai",
    "install-client": "cd client && npm install",
    "install-server-ai": "cd server && npm install @google/generative-ai multer dotenv",
    "dev": "concurrently --kill-others \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}